profiles {
    docker {
        docker.enabled = true
    }

    awsbatch {
        process.executor = 'awsbatch'
        process.queue = 'nextflow-awsbatch-queue'
        aws.region = 'ap-southeast-1'
    }
}
